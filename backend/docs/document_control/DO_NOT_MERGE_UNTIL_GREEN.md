# Release gate reminder

PR #358 must remain draft until the clean PostgreSQL migration, mapper/route checks, full backend regressions, production frontend build, source-contract checks, and live browser acceptance are complete on a branch synchronized with `main`.
