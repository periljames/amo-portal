import type { DashboardConfig } from "./types";

const safetyDashboardConfig: DashboardConfig = {
  id: "safety",
  title: "Safety cockpit",
  departments: ["safety"],
  description: "Safety-focused cockpit with compliance, training, and risk signals.",
};

export default safetyDashboardConfig;
